//! Web integration mapping for Java `SaTokenContextFilterForServlet`.
//! Responsibility is implemented by the `axum` adapter instead of Spring/Servlet crates.
pub use crate::layer::{SaTokenLayer, SaTokenService};
